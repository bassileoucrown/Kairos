# Render Blueprint for the Kairos app (the `app/` directory).
#
# One web service plus a Postgres database. In production the Express server
# also serves the built client and hands unknown paths back to index.html for
# client-side routing (see app/server/index.js).
#
# To deploy: Render Dashboard -> New -> Blueprint -> pick this repo -> Apply.
# It has to be New -> Blueprint, not a redeploy of an existing web service: a
# redeploy re-runs the old service definition and never creates the database
# below, leaving the app on the ephemeral SQLite this file exists to replace.
#
# The database starts empty. Accounts made against an earlier SQLite deployment
# do not carry over and cannot be recovered — sign up once more after the first
# Blueprint deploy. From then on the account survives restarts and deploys.
#
# The database is the point of this file. Render web instances have an
# ephemeral filesystem: a SQLite file there is wiped on every restart and
# redeploy, taking every account with it — which looks exactly like "the app
# doesn't recognise my email". DATABASE_URL below is wired from the managed
# Postgres instance, and app/server/lib/db.js switches backends on its
# presence, so data survives restarts, deploys, and the free plan's idle sleep.
databases:
  - name: kairos-db
    databaseName: kairos
    plan: free
    # Free Postgres expires 30 days after creation, then allows a 14-day grace
    # period to upgrade before Render deletes it and its data — roughly 44 days
    # in total. Render emails warnings at both points, and an account may hold
    # only one free database at a time.
    #
    # The harder limit is that free Postgres has no backups at all. Fine while
    # evaluating; move to a paid instance before this holds a real principal's
    # calendar and contacts, whatever the expiry clock says.

services:
  - type: web
    name: kairos
    runtime: node
    plan: free
    rootDir: app
    buildCommand: cd client && npm install && npm run build && cd ../server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: kairos-db
          property: connectionString
      # server/lib/db.js also uses node:sqlite for local development, which
      # needs Node 22+.
      - key: NODE_VERSION
        value: "22"
      # Optional: set a Resend API key in the Render dashboard to send real
      # email. Without one, every message is still written to the in-app
      # Outbox tab and the server log, and the password-reset screen says so
      # plainly rather than pointing you at an inbox that will never receive
      # anything.
