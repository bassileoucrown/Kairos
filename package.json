{
  "name": "kairos-landing",
  "version": "1.0.0",
  "description": "Landing page and waitlist for Kairos by Exousia — the scheduling OS for those whose time matters most.",
  "private": true,
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "bash app/tests/allsuites.sh"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1"
  },
  "engines": {
    "node": ">=18"
  }
}
